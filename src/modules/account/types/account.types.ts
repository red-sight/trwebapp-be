export enum ECurrencyType {
  REAL = 'real',
  BONUS = 'bonus',
}

export enum ESystemAccountType {
  DEPOSITS = 'deposits',
  FEES = 'fees',
}

export enum EAccountType {
  USER = 'user',
  SYSTEM = 'system',
}
